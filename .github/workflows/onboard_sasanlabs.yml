name: First Time Contributor Form

on:
  workflow_dispatch:

jobs:
  comment-form:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled in fork
        run: echo "Onboarding workflow disabled in research fork."
